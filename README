Basic diff of CPAN modules installed on two hosts via SSH. 

Usage: ./CompareHostCpanModules.pl login@host1 login@host2

Script ssh's into both hosts and grabs a list of installed CPAN modules and versions, then outputs the differences
